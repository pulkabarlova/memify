<?xml version="1.0" encoding="utf-8"?>
<!-- Разрешает нешифрованный HTTP только для локальной разработки. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <!-- Тестируешь с реального телефона по Wi-Fi (не эмулятор)? Добавь сюда IP своего
             компьютера в локальной сети, например: <domain includeSubdomains="false">192.168.1.42</domain>
             (и не забудь поменять ApiConfig.baseUrl на этот же адрес). -->
    </domain-config>
</network-security-config>
